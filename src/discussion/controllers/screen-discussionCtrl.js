/**
* Controller s'occupant de l'affichage des messages
*
*/
discussionModule.controller('screenDiscussionCtrl', ['$scope', function($scope){
	$scope.isHidden = true;
	// Ecouteurs d'événements
	// $scope.$on('add_contact', function (event, dataContact) {
	// 	console.log(dataContact);
	// })
}])